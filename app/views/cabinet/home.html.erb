<div>
На вашем счету: <%= @user.money %> руб.
</div>

<h2>Список серверов</h2>
<table class="server-list">
  <thead>
    <tr>
      <td>ID</td>
      <th>Доменное имя</th>
      <th>IP</th>
      <th>Количество слотов</th>
      <th>До какого оплачено</th>
      <th>Статус</th>
      
    </tr>
  </thead>

  <tbody>
    <% @user.tsservers.each do |ts| %>
      <tr>
        <td><%= ts.id %></td>
        <td><%= ts.dns+'.easy-ts3.ru' unless ts.dns.nil? %></td>
        <td><%= @ip + ':' + ts.port.to_s %></td>
        <td><%= ts.slots %></td>
        <td><%= ts.time_payment %></td>
        <td><%= @status.server_status(ts.machine_id) %></td>
        <td><%= link_to 'Удалить',  {controller: 'cabinet', action: 'destroy', id: ts}, method: :delete, data: { confirm: 'Are you sure?' }  %></td>
        <td><%= link_to 'Продлить', {controller: "cabinet", action: "extend", id: ts.id}%></td>
      </tr>
    <% end %>
  </tbody>
</table>

<div class="btn btn-default">
<%= link_to 'Купить сервер', cabinet_new_path %>

</div>


<div>
<%= request.remote_ip %>

</div>



