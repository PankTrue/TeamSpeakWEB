<h2><%= @user.email %></h2>

<%= form_tag "/admin/setmoney/#{@user.id}", method: :post do %>
    Баланс:<%= number_field_tag 'money', @user.money, class: :text_field, step: :any  %>
    <%= submit_tag 'Save', id: @user.id, remote:true %>
<% end %>
Всего потрачего пользователем: <%= @user.spent %> руб.

<table class="server-list">
  <thead>
  <tr>
    <th>ID</th>
    <th>Домен</th>
    <th>Кол-во слотов</th>
    <th>До какого оплачено</th>
    <th>user_id</th>
    <th>machine_id</th>
    <th>Дата создания</th>
    <th>Дата редактирования</th>
    <th>state</th>

  </tr>
  </thead>

  <tbody>
  <% @user.tsservers.each do |ts| %>
      <tr>
        <td><%= ts.id %></td>
        <td><%= ts.dns+'.'+Settings.other.dns unless ts.dns.empty? %></td>
        <td><%= ts.slots %></td>
        <td><%= ts.time_payment %></td>
        <td><%= ts.user_id %></td>
        <td><%= ts.machine_id %></td>
        <td><%= ts.created_at %></td>
        <td><%= ts.updated_at %></td>
        <td><%= ts.state %></td>
        <td><%= link_to 'Панель', cabinet_panel_path(id: ts.id), class: 'btn btn-default' %></td>

      </tr>
  </tbody>
  <% end %>
</table>

